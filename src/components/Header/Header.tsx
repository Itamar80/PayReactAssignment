import { FC } from 'react';
import './Header.css'

type Props = {
}


export const Header: FC<Props> = ({ }): JSX.Element => {
    return (
        <div className='header-container'>
            header
        </div>
    )
}