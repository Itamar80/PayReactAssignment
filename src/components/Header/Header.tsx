import { FC } from 'react';
import './Header.css'

export const Header: FC = (): JSX.Element => {
    return (
        <header className='header-container'>
            <h1>Pay.</h1>
        </header>
    )
}